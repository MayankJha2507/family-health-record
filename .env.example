# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never expose to the client. Used by the extraction pipeline (Phase 1).
SUPABASE_SERVICE_ROLE_KEY=

# Parsing (Phase 1). Groq = text structuring; Gemini = scanned/image PDFs only.
GROQ_API_KEY=
GEMINI_API_KEY=
